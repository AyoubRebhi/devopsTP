# Required metadata
sonar.projectKey=devopsTP
sonar.projectName=DevOps Training Project
sonar.projectVersion=1.0

# Path settings
sonar.sources=src
sonar.tests=tests
sonar.sourceEncoding=UTF-8

# Language (adjust if needed)
sonar.language=java

# Optional: exclude files or folders
# sonar.exclusions=**/*.xml,**/test/**

# Optional: set the scanner to use
# sonar.java.binaries=target/classes
